<!DOCTYPE html>
<html lang="en">

	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Bootstrap demo</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	</head>

	<body>
		<header>
			{# Creation de la Navbar #}
			<nav class="navbar navbar-expand-lg bg-dark">
				<div class="container-fluid">
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarNav">
						<ul class="navbar-nav">
							<li
								class="nav-item">
								{# On vient indiquer sur le href, la destination de l'url lors du clic, ici --> home #}
								<a class="nav-link active text-light" aria-current="page" href="home">Médiathèque</a>
							</li>
							<li
								class="nav-item">
								{# On vient indiquer sur le href, la destination de l'url lors du clic, ici --> home #}
								<a class="nav-link text-secondary" href="home">Accueil</a>
							</li>
							<li
								class="nav-item">
								{# On vient indiquer sur le href, la destination de l'url lors du clic, ici --> Compte #}
								<a class="nav-link text-secondary" href="compte">Compte</a>
							</li>
						</ul>
					</div>
					{# formulaire de recherche de film #}
					{# On pense a passer en méthode="post" (méthode get de base) afin d'etre en cohérence avec la méthode $_POST #}
					<form action="" method="post" class="d-flex" role="search">
						<input name="recherche" class="form-control me-2" type="search" placeholder="Rechercher un film" aria-label="Search">
						<button class="btn btn-outline-light" type="submit">Rechercher</button>
					</form>
				</div>
			</nav>
		</header>
	</body>
</html>
